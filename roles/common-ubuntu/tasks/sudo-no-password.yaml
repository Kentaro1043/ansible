---
- name: Make sudo no password for sudo group
  become: true
  community.general.sudoers:
    name: sudo group no password
    commands: ALL
    group: sudo
    nopassword: true
    state: present
    validation: required
