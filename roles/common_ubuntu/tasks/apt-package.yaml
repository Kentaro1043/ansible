---
- name: Install common packages
  ansible.builtin.apt:
    name:
      - git
      - vim
      - gpg
      - acl
      - lsof
      - iptables-persistent
    update_cache: true
    state: present
